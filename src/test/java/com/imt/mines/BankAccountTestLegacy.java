package com.imt.mines;

import junit.framework.TestCase;

// Legacy/duplicate placeholder to avoid duplicate-class compilation errors while tests are reorganized.
public class BankAccountTestLegacy extends TestCase {

    public void testPlaceholder() {
        // trivial placeholder test
        assertTrue(true);
    }
}
